# The domain root is the only place a robots.txt is read from. `artivy` is a Pages
# *project* site served at /artivy/, so a robots.txt committed there would exist and
# never be fetched — the same reason app-ads.txt has to live in this repo.
#
# Two sitemaps, one per repo, each covering only its own directory: this repo serves
# the single page at the root, and the `artivy` repo serves everything under /artivy/.
# A sitemap is only unambiguously authoritative for URLs inside its own path, so they
# are kept separate rather than one cross-submitting the other's URLs.

User-agent: *
Allow: /

Sitemap: https://ndri-nr.github.io/sitemap.xml
Sitemap: https://ndri-nr.github.io/artivy/sitemap.xml
